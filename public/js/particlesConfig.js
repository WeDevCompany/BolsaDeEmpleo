window.onload = function() {
  Particles.init({ options });
};

// e.g.
window.onload = function() {
  Particles.init({
    selector: '#myCanvas',
    color: '#B0BEC5',
    sizeVariations: '5',
    connectParticles: true
  });
};